
// import {Op, pub} from "@benev/slate"
// import {house} from "../house.js"
// import {HandleJoin, HostState, JoinerControls, JoinerHandlers, createSessionAsHost, standardRtcConfig} from "sparrow-rtc"

// // export type NetworkOptions = null | NetworkHostOptions | NetworkClientOptions

// // export type NetworkHostOptions = {
// // 	type: "host"
// // 	label: string
// // }

// // export type NetworkClientOptions = {
// // 	type: "client"
// // 	sessionId: string
// // }

// export const networkSystem = (options: NetworkOptions) => house.system(({entities}) => {
// 	if (options) {
// 		if (options.type === "host") {
// 			const session = createSessionAsHost({})
// 		}
// 		else {

// 		}
// 	}

// 	const [,{network}] = entities.singletonRequired("network")

// 	if (network.target) {
// 		if (network.target.type === "host") {
// 			createSessionAsHost({

// 			})
// 		}
// 		else {}
// 	}

// 	return _tick => {
// 	}
// })

